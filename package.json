{
  "name": "omp-feishu",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "飞书对话直接用 omp，也可挂本机 collab 会话看完整流程",
  "scripts": {
    "start": "bun src/index.ts",
    "dev": "bun --watch src/index.ts",
    "list": "bun src/index.ts list",
    "test": "bun test"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "dependencies": {
    "@larksuiteoapi/node-sdk": "^1.55.0"
  }
}
